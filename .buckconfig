[alias]
  gerrit-oauth-provider = //:gerrit-oauth-provider
  plugin = //:gerrit-oauth-provider

[java]
  src_roots = java, resources

[project]
  ignore = .git

[cache]
  mode = dir
  dir = buck-out/cache
